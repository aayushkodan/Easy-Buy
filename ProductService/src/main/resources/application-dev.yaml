spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: root
    url: jdbc:postgresql://localhost:5432/productdb

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 20MB

aws:
  s3:
    region: ${S3_REGION:ap-south-1}
    bucket-name: ${S3_BUCKET_NAME:s3-easy-buy}
    access-key: ${S3_ACCESS_KEY:AKIATWBJ2KI76HEIOZUD}
    secret-key: ${S3_SECRET_KEY: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}

eureka:
  client:
    service-url:
        defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

logging:
  level:
    com.aayush.productservice: DEBUG

